# Baked into Netlify production builds (override in Netlify UI if needed)
VITE_MANIFEST_URL=https://radiant-dusk-c1027e.netlify.app/tonconnect-manifest.json

# REQUIRED for sale/mint on Netlify: public URL of alamdar-contract/backend
# Example: VITE_API_URL=https://alamdar-api.example.com
# Leave unset only for local `npm run dev` (uses Vite /api proxy).
# VITE_API_URL=
